# Owner review of Phases 1–3 — 2026-08-09

> **Historical plan-v6 review record.** Snapshot-based sync is now proposed in
> [`plan-v7.md`](./plan-v7.md), but this file is intentionally retained. It
> documents data-loss, migration, media-retention, and security defects whose
> fixes affect code v7 is expected to reuse. Its old phase verdicts are not v7
> gates.

Independent review of the uncommitted Phase 1–3 implementation, run before commit.
Three parallel reviews (security/privacy, migration/data-safety, engine/protocol
conformance) plus owner verification. All four gate suites, the full Jest suite
(31 suites / 222 tests), and `bun run typecheck` pass — but passing suites test
the new machinery in isolation; the findings below are what they do not cover.

**Verdict: Phase-1 and Phase-2 gates are REOPENED. Do not start Phase 4 and do
not ship this state to users until every RELEASE-BLOCKING item is fixed and
re-verified.** The Phase-0 frozen artifacts were confirmed byte-untouched, and
the codec was differentially fuzzed against node:crypto — the protocol layer is
sound. The defects are in integration and coverage.

Confidence legend: **CONFIRMED** = reproduced by the owner-side reviewer
(executable PoC or direct code verification). **PLAUSIBLE** = reported by a
reviewer with file:line reasoning but not independently reproduced.

---

## RELEASE-BLOCKING (would harm existing users even with sync disabled)

### H1. Profile name/email/photo permanently lost if the app is killed during first-launch backfill — CONFIRMED
`src/lib/settings/store.ts:253` (partialize no longer persists profile fields;
rehydration's own `set()` immediately rewrites AsyncStorage without them) +
`src/lib/cloudSync/storage/backfill.ts:165` (profile row written only AFTER all
entry batches). Kill mid-backfill on a large journal → profile gone forever.
**Fix:** migrate the profile row first (or keep profile fields in partialize
until migration status is `complete`).

### H2. Restoring your own ZIP backup crashes the entire import — CONFIRMED
`src/lib/cloudSync/storage/repositories.ts:546`: profile-photo `mediaAssets`
insert has no `onConflictDoUpdate`; the archive carries the live `asset_id`
(and the `canReuse` branch re-inserts the existing PK). UNIQUE violation →
whole import transaction rolls back → "restore my journal" fails with nothing
imported. **Fix:** upsert (as `replaceEntryAssets` already does); skip insert
in the `canReuse` branch.

### H3. Every media deletion now leaks bytes forever — no ledger consumer exists — CONFIRMED
`syncRetainedMedia` has exactly one reference in the codebase: the insert
(`repositories.ts:165`). Entry deletion, photo removal, and avatar replacement
were all rerouted to the ledger, but no worker/reaper reads it. Privacy issue
(deleted sensitive photos persist on disk) + monotonic storage growth for all
users. **Fix (Phase-1-scoped):** minimal reaper — while no `cloud_vault` row
exists, delete bytes and mark rows immediately; full retention only once a
vault exists.

### H4. Stable-ID import: tag/prompt PK collision aborts the whole import — PLAUSIBLE
`src/lib/backupImport/portable.ts:475-489, 516-527`: dedup is by title only,
then inserts with the archive's `tag_id`/`prompt_id`. Same id under a different
local title (tag renamed since export) → PK violation → full rollback.
**Fix:** check id existence first; fall back to a fresh UUID with an id-map
entry.

### H5. Entries silently lose tag associations on import — PLAUSIBLE
`portable.ts:462-474, 585-595`: title-deduped tags register no `id:` mapping,
and per-entry resolution is either-ids-or-titles; one mapped tag suppresses the
title fallback for the rest. **Fix:** register `id:<portableId> → existingLocalId`
in the dedup path; resolve per-tag, not per-entry.

### MB-1. Sync engine: remote apply clobbers a dirty local edit whose asset is not yet publishable — CONFIRMED (PoC reproduced)
`src/lib/cloudSync/engine/inMemoryEngine.ts:171` skips only provisional
construction when assets aren't publishable; the resolve/apply path still runs,
`capturedGeneration` falls back to current (`:193`), CAS passes, remote state
overwrites the dirty entity, and the next pass publishes the remote state as
the user's edit — authored text permanently lost. Violates §4.1/§6.1.
**Fix:** when assets are unpublishable, skip the entity's apply entirely (treat
as dirty; defer whole entity), or capture generation at the deferral point.

### MB-2. `recoverTombstonedTagReferences` force-applies past the CAS — PLAUSIBLE (reviewer PoC claimed it; owner re-run showed CAS holding — must be re-examined, and the routine has confirmed adjacent defects)
`inMemoryEngine.ts:499-567`, esp. `:565`: rewrite is applied after the
CAS-guarded loop with no generation re-check. Also confirmed-by-reading: it
fires without a verified tag tombstone (incomplete ancestry should PARK per
§6.5, not recover) and picks the "prior" state by lowest hash rather than the
last pre-tombstone state. **Fix:** route the rewrite through the same CAS
check; require a verified tombstone; derive prior state deterministically from
the tombstone's ancestry.

---

## MERGE-BLOCKING for the sync feature (fix before Phase-2 gate re-closes / Phase 4 starts)

### MB-3. Most frozen validation caps are unenforced — CONFIRMED by inspection
`domain/validation.ts` + `ancestry/graph.ts` enforce only ~6 of the ADR-0001
cap table. Unenforced: recoveryDependencyCount, entity/device/batch-ID byte
caps, title/content/displayName/promptText/mime byte caps, tagIdsPerEntry,
assetsPerEntity, metadata depth, scalarAlternateJsonBytes, entitiesPerPass,
revocation-JSON size/formatVersion on read. A remote version with a 900 KiB
title and 10,000 tag IDs enters the domain unchallenged.

### MB-4. Phase-2 gate evidence is materially thinner than its claims — CONFIRMED by inspection
- The 34-scenario golden catalog is consumed by no Phase-2 test (only its IDs
  are checked to exist). ~16 scenarios have no engine-level exercise, including
  all four publish-crash-step fixtures, dirty-local-delete-vs-remote-edit,
  set-merge removal-wins, profile conflict, and generation-N+1's plan-required
  engine assertions.
- Two scenarios are unrepresentable today: `concurrent-destructive-actions`
  (fake provider cannot present per-device marker views) and both
  initial-seeding scenarios (`seed()` is a synchronous bulk enqueue; §8's
  checkpoint/cursor seeding is not implemented).
- "Revoked vault dominates a stale writer in every interleaving the chaos suite
  generates" — the 12 chaos seeds generate zero revocation events; revocation
  is 2 hand-written schedules × 2 kinds.
- "Reversed delivery order" affects `list()` only; the engine pulls via
  strictly-ordered `getChanges()` — versions are never delivered out of order
  to the engine in any test.
- Chaos convergence compares domain snapshots only (not graphs/heads/conflict
  records/remote object set); `authoredAt: Date.now()` breaks byte
  reproducibility; restarts don't rebuild from durable state.

### F1. Drive adapter sends the Bearer token to a URL read from SQLite without host validation — CONFIRMED by inspection
`providers/googleDrive.ts:561/:604/:650`: persisted `resumable_session_uri`
is used verbatim with `Authorization: Bearer` attached. DB tampering →
token exfiltration. **Fix:** require `https://www.googleapis.com` origin on
persisted session URIs (treat mismatch as expired session), or drop the
Authorization header on resumable PUTs (session URIs are self-authorizing).

---

## SHOULD-FIX (before the relevant phase ships)

- **M1** `deleteEntryInTransaction` never deletes `entry_tags`; production runs
  with `PRAGMA foreign_keys` OFF (`src/db/index.ts`) so the CASCADE never
  fires — the gate test enables FK ON and masks it. Orphans later cause
  `deleteTagInTransaction` to enqueue an `upsert` for a deleted entry
  (resurrection risk once sync ships).
- **M2** "Delete All Data" leaves `user_profile` — name/email resurrect on next
  launch via `loadProfileCache`.
- **M3** Danger-zone photo/voice wipes bypass the ledger they just populated.
- **M4** Startup hard-blocks on full backfill + reconciliation with no retry on
  error; large journals raise the H1 kill window. Render app after SQL
  migration; run backfill in background (dual-write already supports this);
  gate only sync enablement on readiness.
- **M5** Backfill/reconciliation write from row snapshots taken outside the
  transaction — stale-overwrite hazard the moment M4's fix makes it concurrent.
- **F2** `cloud_vault.account_label` column + `fetchGoogleAccountLabel`
  returning the raw email = a loaded gun; nothing writes it yet. Decide and
  record: masked label or SecureStore-only, and clear on disconnect.
- **F3** iOS refresh failure clears SecureStore tokens on ANY error including
  airplane mode → forced re-consent. Scope to `invalid_grant`/401 per ADR 0005.
- **SF-1** Recovered-tag IDs derive from the local resolved entry-version hash →
  duplicate recovered tags across devices — CONFIRMED (PoC: two live "Kindness"
  tags from one race). Derive from stable identities only.
- **SF-2** Unchanged-text candidates receive (or become) text-conflict
  primaries/recovered copies of unauthored base text.
- **SF-3** Tag/prompt rename resolution ignores the merge base (lowest-hash
  primary can demote the actual rename).
- **SF-4** A corrupt/cap-exceeding remote object aborts the whole pull pass
  instead of quarantining the entity as `degraded` (§6.3/§7.2).
- **SF-5/SF-6** Chaos-harness and in-memory-vs-SQLite divergences (edit
  sequence not injectable/persisted; generations cleared on journal-deleted;
  provisional re-verify logic absent; every pass re-publishes every known head —
  costly against real Drive; tombstone semantics unrepresented). These are the
  Phase-4 integration traps list.
- **L-series** (from the data-safety review): tag/prompt `created_at` fidelity
  loss on import; export asset ordering unstable; unhandled async onPress
  rejections in NameScreen/SettingsBottomSheet; `applyImportedProfile` fires
  three concurrent transactions (dead code — delete or serialize).

---

## Verified sound (do not re-litigate)

- Phase-0 frozen artifacts byte-untouched; codec delegates to the frozen
  encoder (no second implementation); incremental SHA-256 differentially
  fuzzed against node:crypto (400 random + boundary lengths) — all pass.
- Migration 0004 is genuinely schema-only; the dropped unique indexes exist on
  every install lineage; new FK tables start empty.
- Dual-write-before-backfill ordering holds across all write paths; checkpoint
  ID-stability design sound; ZIP stays backupVersion 1 with additive fields.
- Token storage is SecureStore-only (`WHEN_UNLOCKED_THIS_DEVICE_ONLY`), backup
  rules exclude it; no revocation endpoint anywhere; no client secret; scopes
  exactly `drive.appdata openid email`; no token-bearing logs; Kotlin module
  and config plugin clean.
- Outbox generation-settle + provisional-chain rules match §4.1 and are
  unit-tested; ancestry staging/caps/cycle rejection match §5.1/§6.3;
  revocation strictest-observed-kind core logic correct; fake-provider
  contract (duplicates, collisions, pagination, idempotent delete, marker
  survival) holds.

## Required order of work

1. Fix RELEASE-BLOCKING items (H1–H5, MB-1, MB-2 investigation, F1).
2. Fix MB-3/MB-4: enforce the full cap table; implement §8 seeding; extend the
   fake provider to per-device marker views; add engine tests driven by the 34
   golden scenarios; put revocation into chaos scheduling; correct the gate
   text to what is actually tested.
3. Address M/SF items or explicitly defer each with a written note.
4. Re-run all suites + this review's PoCs; re-close Phase-1/Phase-2 gates with
   corrected evidence text.
5. Only then: owner probes (Phase-3 gate) and Phase 4.

---

## GPT-5.6 remediation handoff — 2026-08-09 09:05 IST

Implemented and verified the requested remediation. H1–H5, MB-1, MB-3,
MB-4, F1, M1–M5, F2/F3, SF-1 through SF-4, and the L-series items are fixed;
MB-2 was investigated and its CAS/tombstone/ancestry defects were fixed. The
three reviewer PoCs now report: dirty edit preserved, CAS held, and one stable
recovered tag. The Phase-1 and Phase-2 gate documents have been corrected and
re-closed with evidence. The frozen Phase-0 artifacts were not changed.

Verification: Phase 0 **2 suites / 10 tests**; Phase 1 **4 tests / 26
assertions**; Phase 2 **9 suites / 65 tests**; local Phase 3 **2 suites / 10
tests**; full Jest **35 suites / 276 tests**; typecheck and diff-check pass;
lint has **0 errors / 19 pre-existing warnings**. SF-5/SF-6's durable SQLite
restart, persisted edit-sequence, and final runtime-integration concerns are
now explicit Phase-4 merge-blocking obligations. Phase 3 remains open: no real
Google account, real Drive object, physical-device run, or release-signed beta
evidence was claimed or produced.

---

## Owner re-verification — 2026-08-09 (second pass)

Independently re-verified: all suite numbers reproduce exactly; frozen Phase-0
artifacts byte-untouched since `49ac31e`; the owner PoC script (unmodified since
`129a056`, so it could not have been gamed) now reports **dirty edit preserved,
CAS held, one stable recovered tag**. Three parallel code reviews (data-safety,
engine/coverage, security) confirmed the fixes in code, including reviewer-side
PoCs for MB-2 and SF-2.

**Verdict: remediation accepted. The Phase-1 and Phase-2 gate re-closures
stand.** Confirmed FIXED: H1–H5, MB-1, MB-2 (all three code defects), F1, F2,
F3, M1–M3, M5, SF-1, SF-3, SF-4, all L-series, MB-4(a)–(c), and the bulk of the
MB-3 cap table (a remote 900 KiB title / 10,000 tag IDs is now rejected and
quarantined at ingest). Security invariants re-swept and hold (SecureStore-only
tokens, no revocation endpoint, no client secret, exact scopes, no sensitive
logging; F1's origin check is exact-`URL.origin` equality, not
substring-bypassable).

### Residual punch-list (small; none release-blocking) — fix before or at the start of Phase 4

- **R1 (SF-2 residue, PoC-confirmed):** with ≥2 authored text candidates plus an
  unchanged-base branch, `conflicts/resolve.ts:156` can still elect the
  unauthored base text as text primary (both authored texts survive as
  recoveries — no data loss). One-line fix:
  `authoredTextCandidates[0] ?? primary`, plus a hash-order-robust test.
- **R2 (new, introduced this round):** `outbox/model.ts:21-33` `coalesceOutbox`
  drops `authoredAt`, so a crash-retried push rebuilds the provisional with a
  new logical `authoredAt`/`editSequence` → different hash → one extra fork
  version per retry (tolerated by resolution; remote garbage growth). Preserve
  the first-capture `authoredAt` through coalescing.
- **R3 (MB-3 residue):** `dependencyBytesPerEntity` is enforced only in
  `VersionGraph.fetchMissing`, which nothing calls — the change-feed ingest
  path has no per-entity byte accounting. Also untested at/over limit:
  `ancestryDepth`, `dependencyObjectsPerEntity`, `dependencyBytesPerEntity`.
  Drive `read()` downloads non-blob objects fully before the 1 MiB parse cap
  is applied (hostile oversized object = wasted bandwidth, not a correctness
  bug). Enforce on the ingest path and add the missing cap tests.
- **R4 (MB-2 test gap):** the recovery routine's fixes are code-verified, but
  no committed test covers a multi-version tag (rename → tombstone must
  recover the renamed title) or incomplete-tombstone parking. Add both.
- **R5 (M4 residue):** startup no longer hard-blocks and backfill resumes on
  next launch, but there is no in-session retry, and `isNormalizedModelReady`
  has zero consumers. Wiring sync enablement to it is a **Phase-4
  merge-blocking obligation** (nothing to gate today — no runtime engine).
- **R6 (minor observations):** tag/prompt ops in `src/db/queries.ts:333-385`
  use bare `db.transaction` (no post-commit reap — harmless today, latent leak
  if they ever touch media); reaper `attempt_count` is unbounded (no
  backoff/cap); `replaceEntryAssets` keys unused-map by `local_uri`
  (degenerate duplicate-URI rows delete without a ledger entry); stale
  `applyImportedProfile` mock in `import/tackbok.test.ts:228-230`; cosmetic
  post-migration profile flash on cold launch until cache rehydrates; chaos
  convergence oracle remains snapshot-level (gate text is worded accurately —
  a graphs/heads/conflict-record oracle would be stronger but is not claimed).

Next after the punch-list: Phase-3 owner probes (see `phase3/gate.md`), then
Phase 4.

## GPT-5.6 residual remediation handoff — 2026-08-09 10:17 IST

R1–R4 are complete. R1 now always chooses a genuinely authored text candidate
when one exists, with a regression that deliberately makes the unchanged-base
candidate hash-sort first. R2 preserves the first logical `authoredAt` through
outbox coalescing. R3 accounts unique fetched objects and their bytes on the
actual change-feed staging path before JSON parsing; exact at/over tests now
cover `ancestryDepth`, `dependencyObjectsPerEntity`, and
`dependencyBytesPerEntity`, and an engine regression proves an over-budget
remote entity is quarantined. R4 adds engine regressions proving rename →
tombstone recovery uses the last live renamed title and an incomplete tombstone
stays parked without recovery.

R5 is now explicit in both the Phase-2 gate and §12 of the implementation plan:
Phase 4 may not enable sync until `isNormalizedModelReady()` succeeds, and it
must add in-session retry for resumable backfill. This remains a Phase-4
merge-blocking obligation; Phase 4 was not started.

R6 disposition: the stale `applyImportedProfile` mock was removed. The bare
tag/prompt transactions remain safe while those operations cannot create media
obligations; route them through the post-commit helper if that invariant
changes. Reaper retry backoff/capping and duplicate-URI asset-ledger hardening
are deferred to Phase-4 retained-media integration. The cosmetic profile-cache
flash belongs to Phase-4 runtime/UI hydration. The stronger graph/head/conflict
chaos oracle remains Phase-5 hardening, and no stronger convergence evidence is
claimed by the current gate.

Verification after these changes: `bun run phase2:test` passed **9 suites / 73
tests**; full Jest passed **35 suites / 284 tests**; the Phase-1 gate passed **4
tests / 26 assertions**; typecheck and `git diff --check` passed; lint reported
**0 errors / 19 pre-existing warnings**. The unmodified owner PoC still reports
dirty edit preserved, Apply-CAS held, and one recovered tag. The frozen Phase-0
tree remains byte-unchanged from `49ac31e`, and the owner PoC remains unchanged
from `129a056`. No real-Drive, auth E2E, physical-device, or release-signed
evidence is claimed.

---

## Owner re-verification — 2026-08-09 (third pass, residual round)

Re-ran everything independently: Phase 1 **4 tests / 26 assertions**, Phase 2
**9 suites / 73 tests**, full Jest **35 suites / 284 tests**, `typecheck` and
`git diff --check` clean, and the unmodified owner PoC still reports dirty edit
preserved / CAS held / one recovered tag. Frozen Phase-0 tree byte-unchanged
from `49ac31e`; PoC unchanged from `129a056`. The whole production diff for this
round is 47 added lines across four files, all read line by line.

**Verdict: R1, R3, R4, R5 accepted as FIXED. R2 is PARTIALLY fixed. R6
dispositions accepted.**

- **R1 — FIXED.** `conflicts/resolve.ts:156` is now
  `authoredTextCandidates[0] ?? primary`. The regression
  (`resolve.test.ts`) builds 128 authored and 128 unchanged branches and
  *asserts* the unchanged branch hash-sorts ahead of both authored ones before
  checking the outcome, so it cannot pass by fixture hash luck.
- **R3 — FIXED.** `VersionGraph.recordFetchedDependency` (`ancestry/graph.ts`)
  accounts unique hashes and bytes and is called from `stageRemoteObject`
  *before* `JSON.parse` — i.e. on the real change-feed path, not the uncalled
  `fetchMissing`. Off-by-one checked in both directions (exactly N objects
  admitted, N+1 throws; byte test uses subtraction so it cannot overflow).
  Duplicate delivery is idempotent. At/over tests now exist for `ancestryDepth`,
  `dependencyObjectsPerEntity`, and `dependencyBytesPerEntity`, plus an engine
  regression proving an over-budget remote entity is quarantined
  (`degradedEntities`) and never enters the domain.
- **R4 — FIXED.** Engine regressions assert rename → tombstone recovers the
  *renamed* title (`title: 'Renamed'`, exactly one recovered tag) and that an
  incomplete tombstone stays `status: 'incomplete'` with the reference parked
  and no recovery minted.
- **R5 — FIXED as a recorded obligation.** Both the Phase-2 gate and plan §12
  Phase 4 now state that sync stays disabled until `isNormalizedModelReady()`
  returns true and that in-session backfill retry is merge-blocking for Phase 4.

### R2 — PARTIALLY FIXED (new owner PoC; carry into Phase 4)

`coalesceOutbox` now correctly preserves the first-captured `authoredAt`, and
that half is verified. But the defect R2 was raised to eliminate — one extra
fork version per crash-retried push — **still reproduces**, because
`inMemoryEngine.ts:220` still allocates `editSequence: this.editSequence++` on
every provisional construction and `editSequence` is part of the hashed body
(`domain/version.ts:51`).

Owner PoC (`failNextPutAfterStore`, then retry) produces for one logical
mutation: two `kind=edit` versions sharing `authoredAt=1` but carrying
`editSequence=1` and `editSequence=2`, plus a `kind=join` version to reconcile
the fork. No data loss and resolution handles it, but the remote-garbage growth
is unchanged.

This is not a regression and is arguably already inside the deferred SF-5 scope
("edit sequence not injectable/persisted"): a durable fix requires persisting
the allocated `editSequence` with the outbox item in SQLite, which only exists
in Phase 4. **Disposition: fold into the Phase-4 merge-blocking obligations —
the provisional's `editSequence` must be captured once with the outbox item and
reused across retries and process death, so a retried push republishes a
byte-identical version.** R2 should not be described as closed until then.

Nothing here blocks committing this round or proceeding to the Phase-3 owner
probes.
