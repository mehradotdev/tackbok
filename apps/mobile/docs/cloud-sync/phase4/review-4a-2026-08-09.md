# Phase-4a owner review — 2026-08-09

Reviewer: the reviewing agent, at the owner's request, against the working tree
of `feat.cloud-sync` (uncommitted Phase-4a work on top of `5847f7a`).

Verdict: **changes requested.** The gate's ticked items are individually
truthful about what was executed, but two of them do not discharge the
obligation they are standing in for, and the durable design does not survive
contact with the scale the plan targets. Phase 4b should not start yet.

Everything checked below re-ran clean: `tsc --noEmit`, `bun run test`
(39 suites / 335 tests), `bun run phase4:test` (73 Jest + 83 Bun),
`bun run lint` (0 errors, 19 pre-existing warnings).

## Blocking

### B1 — the checkpoint is super-quadratic in vault size

`InMemorySyncDevice.toDurableSnapshot()` serializes the **entire** engine —
`domain`, every `VersionGraph`, `outbox`, `appliedHeads`, `conflicts`,
`seedItems`, the staged change page — and `checkpoint()` is invoked on every
mutation, every pulled page, every apply, every publish step, every purge
batch, and every settle. Each of those calls stringifies the whole state and
rewrites one SQLite row.

Measured, on this machine, seeding a fresh vault of ~400-character entries
through `SQLiteSyncEngine` with the real migration and a real SQLite database:

| entities | passes | checkpoint row | checkpoint saves | JSON written | wall clock |
| --- | --- | --- | --- | --- | --- |
| 500 | 11 | 1.3 MB | 4,783 | 4.99 GB | 4.7 s |
| 2,000 | 41 | 5.0 MB | 49,123 | 204.49 GB | 186.2 s |

Growth between those two points is ≈ n^2.7; quadratic is the floor, because
snapshot size is O(n) and the number of saves is itself O(n). 2,000 entries is
about five and a half years of daily journaling — not an edge case — and it
costs three minutes of pure CPU on a fast desktop before any network work.

Against the §13 targets (20,000–50,000 entity-version files, peak JS heap
≤ 250 MB, full text restore ≤ 30 min on a mid-tier 2022 Android):

- at the quadratic floor, 50,000 entities is 625× the 2,000-entity row —
  ~128 TB of JSON serialization and ~32 hours of CPU;
- the single `snapshot_json` row reaches ~125 MB, held simultaneously as a JS
  string, a parsed object graph, and a SQLite TEXT value — over the whole
  restore heap budget before anything else is allocated.

The right design is already written down in the code. `src/db/schema.ts:296`
says of `sync_engine_checkpoints`: *"The normalized/domain and protocol tables
remain authoritative; this row keeps the pass-local cursor, staged page, seed
cursor, and purge cursor together so a process death cannot create a mixed
checkpoint."* That is correct and it is not what the code does — restore reads
exclusively from the blob and the Phase-1 tables are a write-only mirror. Make
the code match the comment: rehydrate `domain`, `generations`, `graphs`,
`outbox`, `appliedHeads` and `conflicts` from `syncVersions`,
`syncEntityState`, `syncConflicts`, `syncChangeQueue` and the normalized domain
tables — which exist for exactly this and are already being written — and keep
only the small pass-local state in the checkpoint row.

### B2 — the 34-scenario catalog never exercises the restore path

`phase4-gate.test.ts` runs each frozen scenario with `SQLiteSyncEngine` as the
device factory, but every scenario constructs each device exactly once, and the
per-device checkpoint store is empty at construction. `store.load()` therefore
returns `null` in all 34 scenarios and `restoreDurableSnapshot()` is never
called.

What the catalog run proves is that *writing* a checkpoint at every step does
not perturb the frozen semantics. That is worth having. It is not the Phase-2
carried obligation, which is durable **restart** from the production engine.
Restore fidelity is currently covered only by the hand-written restart tests —
a handful of single-entity cases against one entity id.

The gate's first ticked item should not read as if the catalog were re-proven
across restart. Fix the coverage rather than the wording: wrap the factory so
each device is torn down and reconstructed from SQLite between operations. The
chaos test already does this (`devices = ['a','b','c'].map(open)` between
passes); the same trick applied to the catalog runner is cheap and is the
single highest-value test in this phase.

### B3 — initial seeding materializes the whole journal, twice

`ProductionRuntimeEngine.sync()` calls `enumerateNormalizedDomain()`, which
loads every entity's full state into memory, and hands the whole array to
`engine.seed()`, which retains it in `seedItems` — and therefore re-serializes
it into the checkpoint row on every subsequent checkpoint until seeding drains.
For the user this design is aimed at, the first sync loads the entire journal
into JS memory and writes it to SQLite tens of thousands of times.

Seeding should be a cursor over the normalized tables (`id > cursor LIMIT
batch`), not a materialized list. The existing `seedCursor` already provides the
resume point.

## Should fix before 4b

- **S1 — nothing triggers a sync when the user saves.** Implemented triggers
  are app-active, connectivity-restored, backgrounding and the periodic task.
  §9 wants network work "debounced ~30–120 s so bursts become one version",
  which presupposes an edit-driven trigger. As written, an entry written while
  the app stays foregrounded waits for the user to background the app — or for
  an OS periodic wake that iOS may defer for hours. 4b's **Sync now** button
  will paper over this in testing; real usage will not.
- **S2 — triggers arriving during a pass are dropped.**
  `SyncRuntime.run()` returns the in-flight promise instead of queueing a
  follow-up pass. The in-flight pass hydrated its outbox at the start, so an
  edit committed mid-pass is neither included nor rescheduled.
- **S3 — the entire React Query cache is invalidated after every pass.**
  `ProductionRuntimeEngine` awaits `onRemoteApplied?.()` unconditionally;
  `_layout.tsx` maps it to `queryClient.invalidateQueries()`. §13 asks for
  invalidation *on remote apply* — gate it on `result.applied > 0`.
- **S4 — the background task is registered for every user.** `_layout.tsx`
  calls `registerCloudSyncBackgroundTask()` on every launch regardless of
  whether a vault is configured, so users who never connect Drive get a
  15-minute OS wake that does nothing. Gate it on a configured vault (and later
  on the Phase-5 feature flag).
- **S5 — per-pass writes are O(whole vault).**
  `persistProductionEngineResult()` upserts every version of every graph on
  every pass, and `materializeProductionDomain()` rewrites every applied entity
  on every pass. Same root cause as B1; both should be driven by what changed
  in the pass.

## Minor

- `VersionGraph.restoreDurableState()` falls back to `splice(0, 1)` when no
  pending version has its parents satisfied, so a corrupt snapshot yields a
  silently incomplete graph instead of throwing.
- `failureCategory()` cannot produce the declared `'offline'` category — it is
  in the type and unreachable in the code.
- `_layout.tsx`'s start/stop effect leaks the first `start()`'s listeners under
  React's dev double-invoke: `stop()` clears `subscriptions` before the still-
  awaiting `start()` assigns them.

## Confirmed good

These were checked specifically because they are the standing constraints, and
they hold:

- `createProductionRuntimeEngine()` is the sole runtime construction path and
  builds `GoogleDriveProvider({ auth: createGoogleAuthorization() })`, so
  Android token minting stays behind `AndroidGoogleAuthorization` and its
  durable connection mark. No global OAuth revocation endpoint was added and
  Disconnect is untouched.
- Analytics payloads are buckets and enums only — no token, account label,
  entity id, hash, filename, URI, or journal field is reachable from them.
- No notification-permission dependency, asserted by a source scan over the
  runtime modules and `_layout.tsx`.
- Media never enters JS memory whole: production registers 1 MiB-chunked file
  sources through `registerBlobSource`, and `persistInlineBlobs` — the only
  path that puts bytes in the checkpoint — is gate-only.
- Both byte-removal paths (entry asset replacement, profile-photo replacement)
  write the retained-media ledger and a completed obligation inside the same
  transaction as the delete.
- `shouldAdoptQueuedGeneration()` correctly handles the crash gap between the
  engine checkpoint and Phase-1 queue settlement, and is directly tested.
- The readiness gate is real: the engine is not constructed until
  `isNormalizedModelReady()` is true, and the launch-only backfill was replaced
  by an in-session retry timer. This satisfies the merge-blocking §12
  obligation.

## Suggested disposition

1. Commit the work as-is — it is a large, mostly-sound body of runtime code and
   losing it to a rework would be worse than carrying it. Re-label the gate
   status from "ready for owner review" to "changes requested" and point it at
   this document.
2. Hand B1–B3 back for a 4a-fix round. B1 is the phase's actual obligation, not
   a performance nit: the reason Phase 2's in-memory proof was declared
   insufficient is precisely the assumption that the production engine would
   not hold and rewrite the whole vault.
3. Re-review, then start 4b. S1–S5 can ride along with the fix round or be
   folded into 4b, owner's call.
4. Add a scale probe to the phase-4 gate with numbers stated against §13. The
   table above is a starting point; it is not device evidence.

## Implementation response — 2026-08-09 22:20 IST

Status: **remediation implemented; requesting owner re-review.** This response
does not alter the review verdict above, close the gate, or authorize Phase 4b.

- **B1:** replaced the full-vault snapshot with a bounded v2 checkpoint that
  contains only pass state (provider/seed/purge cursors, pending captures, and
  pending materialization acknowledgements). Entities, generations, outbox,
  graphs, applied heads, conflicts, dependency accounting, and dirty local
  state now restore from structured SQLite rows. Persistence is entity-delta
  driven; unchanged heads are not reapplied. Migration `0006` adds the three
  small supporting tables. The new scale probe measured:

  | Entities | Saves | Checkpoint JSON written | Largest checkpoint | Wall clock |
  | ---: | ---: | ---: | ---: | ---: |
  | 500 | 570 | 307,745 bytes | 8,730 bytes | 320 ms |
  | 2,000 | 2,280 | 1,231,848 bytes | 8,733 bytes | 2,860 ms |

  The checkpoint stayed flat and total checkpoint bytes grew 4.00× for 4× the
  entities. These are host results only; no physical-device §13 claim is made.
- **B2:** the 34 frozen scenarios now construct a fresh SQLite engine for every
  device interaction and assert that each device store exercised restore.
  Initial seed pages come from a cursor-backed normalized-source test adapter,
  matching the production boundary. The original frozen Phase-2 suite remains
  unchanged and passes 73/73.
- **B3:** production initial seeding now uses `id > cursor LIMIT 50` paging over
  normalized tables. No production call enumerates or retains the whole
  journal. The restart/ahead-of-cursor gate and the 500/2,000 scale probe both
  use page-at-a-time seeding.
- **S1–S5:** routed writes emit a post-commit local-mutation signal; the runtime
  debounces it for 30 seconds; triggers arriving during a pass coalesce into one
  follow-up; React Query invalidation requires a remotely sourced Apply;
  background-task registration is conditional on a live configured vault; and
  production persistence/materialization is driven only by changed/pending
  entity keys.
- **Minor findings:** corrupt durable graph order now throws; `offline` is a
  reachable analytics failure category; runtime lifecycle generations prevent
  late `start()` work from leaking listeners after `stop()`.
- **Additional crash gap found during remediation:** remote Apply remains
  pending until the normalized/UI transaction explicitly acknowledges it. A
  new test kills before that acknowledgement, reconstructs, replays the exact
  entity, acknowledges it, and verifies the next restart is settled.

Re-verification completed: `bun run phase4:test` (**73 Phase-2 tests + 88 Bun
gate tests**), full Jest (**39 suites / 335 tests**), Phase 1 (**4/4**), Phase 3
(**10/10**), TypeScript, Expo public config, and ESLint (**0 errors / 19
pre-existing warnings**). Frozen protocol/Phase-0/Phase-3 probe artifacts were
not modified. Detailed evidence is in
[`gate.md`](./gate.md) and
[`evidence/2026-08-09-machine.json`](./evidence/2026-08-09-machine.json).

## Owner re-review — 2026-08-10

Verdict: **accepted. B1, B2, B3 and S1–S5 are closed.** Phase 4b may start. Four
follow-on items below are recorded as obligations, not as blockers; one of them
(N4) is merge-blocking before the feature is user-reachable.

Independently re-verified, not taken from the response: `tsc --noEmit` clean,
`bun run phase4:test` (73 Jest + 88 Bun, 328 assertions), `bun run test`
(39 suites / 335 tests), `bun run lint` (0 errors / 19 pre-existing warnings),
migration `0006` correctly registered in both `_journal.json` and
`migrations.js`, and `git diff` confirms no change under `protocol/`, `phase0/`,
`phase3/`, or the Phase-0/3 probe scripts.

**B1 — closed.** The design now matches the schema comment that contradicted it.
`toDurableDelta()` emits only keys in `durabilityDirtyKeys`, the v2 checkpoint
carries pass-local state only, and `load()` rehydrates entities, graphs, outbox,
heads and conflicts from `sync_entity_state`, `sync_versions`,
`sync_change_queue`, `sync_conflicts` and the two new `0006` tables. The
`pullAllChanges()` page cap (`entitiesPerPass: 500`) bounds
`pendingMaterializationKeys`, so the checkpoint stays bounded on the restore
path as well as the seeding path the gate probed. Measured independently on a
*restoring* device — the path the gate's probe does not cover — against
`FakeCloudProvider` with the real migrations `0000`–`0006`:

| entities | restore wall | launch load | quiet pass | checkpoint row |
| ---: | ---: | ---: | ---: | ---: |
| 500 | 0.1 s | 14 ms | 0 ms | 294 B |
| 2,000 | 0.6 s | 52 ms | 2 ms | 295 B |
| 5,000 | 3.0 s | 132 ms | 4 ms | 295 B |
| 10,000 | 10.8 s | 272 ms | 10 ms | 296 B |
| 20,000 | 42.8 s | 538 ms | 23 ms | 296 B |

The checkpoint row is **flat at ~296 bytes across a 40× range**. Compare the
pre-remediation figures at the top of this document: 5.0 MB and 204 GB of
serialization at 2,000 entities. The quiet-pass column also closes S5 — a pass
over a restored 20,000-entity vault costs 23 ms, so the early-continue on
unchanged heads works.

**B2 — closed, and more thoroughly than requested.** The catalog factory returns
a `Proxy` that opens a fresh `SQLiteSyncEngine` from SQLite on *every property
access*, and each scenario asserts `stats.restoredLoadCount > 0` per device
store. Restore is now exercised continuously across all 34 frozen scenarios
rather than at a few hand-placed kill points. Seed pages arrive through a
cursor-backed adapter that mirrors the production boundary.

**B3 — closed.** Production seeds via `id > cursor LIMIT 50` over the normalized
tables; `enumerateNormalizedDomain()` is gone from the pass path.

**S1–S5 — all closed**, verified in source: `addMutationListener` →
`schedule('local-mutation')` on a 30 s debounce (S1); `rerunTrigger` coalesces
an in-pass trigger into exactly one follow-up via `runLoop` (S2);
`if (result.remoteApplied > 0) await this.onRemoteApplied?.()` (S3);
`setCloudSyncBackgroundTaskEnabled(configured)` with unregistration for users
without a vault (S4); delta-driven persistence (S5).

**Minor findings — two of three closed.** `VersionGraph.restoreDurableState()`
now throws `Corrupt durable graph ordering or ancestry cycle` instead of
silently truncating. The `_layout.tsx` StrictMode leak is fixed by the
`lifecycle` generation counter in `SyncRuntime.start()`/`stop()`.

### Follow-on items

- **N1 — restore is still quadratic in wall clock, with a far smaller constant.**
  Excluding this probe's per-pass reload artifact (production constructs the
  engine once per runtime start, not once per pass), sync work alone is 7.6 s at
  10,000 entities and 30.4 s at 20,000 — 4.0× for 2× the entities. The cause is
  structural and unchanged by the remediation:
  `inMemoryEngine.ts` sorts and walks `Array.from(this.graphs.keys())` — every
  entity in the vault — on every pass, and a restore takes n/500 passes.
  Extrapolated quadratically, 50,000 entities is ~3.2 min of pure CPU on this
  desktop; on a mid-tier 2022 Android that plausibly lands at 16–32 min against
  §13's ≤ 30 min full-text-restore budget, before any network time. This is
  ~1,000× better than the pre-remediation design and does not block 4a, but it
  is a named risk against a specific §13 number and should be resolved before
  release. The fix is to drive the resolve loop from keys touched in the pass
  rather than the whole graph set.
- **N2 — §13's peak-JS-heap target remains unmeasured.** I could not isolate it
  here: the fake provider holds the entire remote vault in the same process, and
  Bun's GC reclaims across the measurement window, so the heap deltas were not
  trustworthy in either direction and are deliberately not quoted. What is a
  code fact rather than a measurement: `load()` reads every entity row and every
  version body for the vault into JS maps, and the engine holds `domain`,
  `graphs` and `appliedHeads` for the whole vault resident. Whether that fits
  250 MB at 20,000–50,000 entities under Hermes is unknown and is the one §13
  number nobody has evidence for. It needs either a Hermes harness or Phase-6
  device measurement.
- **N3 — `load()` reads three tables without a vault or device filter.**
  `sync_entity_state`, `sync_change_queue` and `sync_conflicts` are queried
  unfiltered, while `sync_versions` is correctly scoped by `vault_id`.
  `clearStructuredState` only fires on a `journal-deleted` revocation, and no
  disconnect-and-reconnect cleanup path exists yet. Today this is unreachable
  because 4a ships no Disconnect UI. It becomes live cross-vault contamination
  the moment Phase 5 lets a user disconnect and connect a different vault:
  stale `appliedHeads` would reference hashes absent from any restored graph.
  Add the scoping or an explicit teardown before that path ships.
- **N4 — the §10 "update together" rule was not honoured; merge-blocking before
  the feature is user-reachable.** `events.ts` gained all six `cloud_sync_*`
  events, but `TRACKED_EVENT_NAMES` in
  [`PrivacyScreen.tsx`](../../../src/screens/onboarding/PrivacyScreen.tsx) — which
  carries the comment *"Keep in sync with src/lib/analytics/events.ts"* and
  tells users *"These are the only events Tackbok records"* — lists none of
  them, and the website policy is untouched. The gate's non-claims section does
  say no UI or policy work was done, so this is correctly deferred rather than
  misreported; it must not be lost. Nothing emits until a vault is configured,
  so the screen is not yet inaccurate.

### Disposition

1. Commit the remediation.
2. Start Phase 4b. Carry N4 into it as merge-blocking, and N3 as a prerequisite
   for the Phase-5 Disconnect path.
3. Keep N1 and N2 as Phase-6 obligations alongside the existing device waivers —
   both are §13 numbers that only hardware can settle.
